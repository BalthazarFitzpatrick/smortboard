{"id": 52, "title": "keep the reviewer's findings when it hits its budget, and stop calling that a crash", "status": "done", "acceptance_criteria": "a reviewer run that ends error_max_budget_usd after a StructuredOutput call keeps those findings; the gate reports a budget stop, not CRASH; the reviewer budget fits a large diff", "verify_command": "uv run pytest tests/test_reviewer.py -q", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #69 2026-09-13; the board restarted on main acd4c39 with it. BUILT 2026-09-13 as smortboard #69 (go relayed from the operator by the smolsmort session): run_process keeps the last StructuredOutput call on RunResult.structured_output, _parse takes it before any blocked code, a budget stop with no answer reads 'the reviewer hit its budget before it gave a verdict', DEFAULT_REVIEW_BUDGET_USD 1.50. after merge + board restart, the train.py card can be answered. PROPOSED 2026-09-12, awaiting the operator's go. MEASURED on smolsmort's train.py card (2cd984d6), event 1330-1332: tests passed, the reviewer called StructuredOutput with findings (one high best_practice on smolsmort/review/train.py:55), then the result was subtype error_max_budget_usd at $0.503 against DEFAULT_REVIEW_BUDGET_USD 0.50 (reviewer.py:49). review_gate recorded approved false, error 'reviewer run did not complete cleanly: CRASH', findings []. so the card blocked REVIEW_REJECTED with nothing to read, and #67's inbox line 'read the findings' points at nothing. the train.py card must not be answered until this lands - an answer re-runs it into the same wall."}
{"id": 53, "title": "keep the suite's output when the test gate times out, and let a repo set its own timeout", "status": "done", "acceptance_criteria": "a gate that hits its timeout records the partial stdout/stderr it captured; the timeout is a per-repo setting defaulting to 600s", "verify_command": "uv run pytest tests/test_gates.py -q", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #69 2026-09-13; the board restarted on main acd4c39 with it. BUILT 2026-09-13 as smortboard #69: the partial stdout/stderr from TimeoutExpired is kept, the named gate container is removed on timeout (subprocess.run only killed the docker client - the suite kept running on the cpu, a likely part of the contention), and gate_timeout_seconds is a BOARD-WIDE setting (unset 600), not per repo as first proposed - per repo needs a migration. after merge + board restart, the housekeeping card can be answered. PROPOSED 2026-09-12, awaiting the operator's go. MEASURED: smolsmort housekeeping (313033b8) and classdefs (2144b4b5) both blocked TESTS_FAILED with exit -1 and the whole output 'the suite did not finish within 600s' (gates.py:27 GATE_TIMEOUT_SECONDS, :102 replaces the output). subprocess.TimeoutExpired carries .stdout/.stderr, so the partial output is available. unverified hypothesis: CPU contention - three card containers and their gates ran at once on the pre-#64 board. classdefs has since reached smolsmort PR #9; housekeeping still waits and must not be answered until this lands."}
{"id": 55, "title": "move dev work onto one board per repo", "status": "queued", "acceptance_criteria": "every active repo (smortboard, smolsmort, a consumer project, smortui, a private project) has a board with its repo registered, a test image that runs its gate offline, and leased cards seeded from its TASKS.jsonl; dev work starts from the board", "verify_command": "GET /api/boards lists one board per repo and /api/preflight is all ok", "blocked_reason_code": null, "last_verified_commit": null, "notes": "THE OPERATOR'S PLAN 2026-09-13: he resumes via the board and uses one session to create a board per repo; from then on dev work goes primarily through the board, small quick side projects stay in sessions. THINGS THAT WILL BITE, all found this session: (1) a card with no lease is refused (#60) - every card needs leases, set via PATCH /api/cards/<id> {\"leases\": [...]}; the orchestrator is told to set them. (2) each repo needs an image whose gate runs --no-sync offline, and nothing ties that image to uv.lock (task 54) - smolsmort-repo:latest now carries ui_base 3ea805c. (3) the live board has STORED worker and orchestrator prompts (v1, 2026-09-12 16:28, by the smolsmort session). a stored prompt replaces the code default whole (prompts.active_prompt), so later code-default edits never reach runs while it exists; the headless rule is appended outside it since #65. (4) the board's default_branch per repo matters: smolsmort's is feature/review-tool, not main. (5) the board process has run as a background task of a Claude session - start it in a terminal of your own for long work, background boards were killed for memory before."}
{"id": 57, "title": "notice when a repo's base branch has merged into main", "status": "queued", "acceptance_criteria": "when a repo's default_branch is fully merged into origin/main, the board says so on the repo and either offers to move the base to main or refuses to open a card PR into the old base", "verify_command": "uv run pytest -q", "blocked_reason_code": null, "last_verified_commit": null, "notes": "the stranding behind task 41's note: smolsmort #9 merged into feature/review-tool after that branch had already merged into main. a card, not a session fix (a consumer-project session's read, agreed). checkable with `git merge-base --is-ancestor origin/<base> origin/main` before a worktree is cut or a PR opened."}
{"id": 1, "title": "capture the smortboard brief and run an Opus planning session", "status": "done", "acceptance_criteria": "CLAUDE.md mission set; a plan exists with unit header blocks, a whole-system test case and a mermaid flowchart; unknowns marked for spikes", "verify_command": "test -f docs/PLAN.md && echo OK", "blocked_reason_code": null, "last_verified_commit": null, "notes": "brief is docs/dev-board-brief.md. plan approved 2026-09-08, lives at docs/PLAN.md. all 28 review questions answered by the operator inline; every [OPEN] and [FLAG] in the brief is resolved in the plan's 'Resolved from the brief's open flags' section. omarchy dropped entirely (a plugin is a QML bar widget, cannot be a standalone app). fish_gate is out of scope - it gets redone anyway, so its stale vendored ui_base fork is not our problem."}
{"id": 2, "title": "S1 spike - can headless claude code be driven as a card runner", "status": "done", "acceptance_criteria": "a scratch worktree runs one real card via `claude -p` non-interactively to completion; we can point at the exact output showing (a) a parseable event stream, (b) token counts, (c) a completion signal distinguishable from a crash, (d) a commit on the branch", "verify_command": "test -f docs/spikes/S1-S2-findings.md && echo OK", "blocked_reason_code": null, "last_verified_commit": null, "notes": "CONFIRMED. `claude -p --output-format stream-json --verbose --permission-mode acceptEdits --allowedTools ... < /dev/null` ran a real card to completion in 15s and met its acceptance criteria. `< /dev/null` is REQUIRED or it stalls 3s waiting on stdin. result event carries subtype/is_error/terminal_reason/stop_reason (completion vs crash), usage + modelUsage (all of section 7 telemetry), total_cost_usd, session_id, permission_denials. Two undocumented-in-plan events found: system/vcs_state_changed gives commit+branch detection free, and rate_limit_event gives quota. Agent Agent SDK fallback NOT needed. One new phase-2 requirement: the agent chose its own branch name (it inherits ~/.claude/CLAUDE.md and so refused to commit to main), so the board must cut the worktree and branch itself and hand over a checkout already on the right branch."}
{"id": 3, "title": "S2 spike - seat contention and quota visibility", "status": "done", "acceptance_criteria": "two headless sessions run against the one interactive subscription seat while it is in use; we can name what happens (queue, clean failure with a detectable signal, or silent degradation) and what quota data is actually readable", "verify_command": "see docs/spikes/S2-findings.md", "blocked_reason_code": null, "last_verified_commit": null, "notes": "Concurrency CONFIRMED: two headless sessions ran simultaneously on the one interactive subscription seat, each in its own worktree, both clean success, no interference. Parallelism is a SCHEDULING problem, not a credentials one. Quota question RESOLVED and the plan's assumption was WRONG - it said the status line is the only surface exposing subscription usage. Every card's stream carries rate_limit_event with unifiedWindows: five_hour and seven_day utilization plus resetsAt, plus a status field. Section 4.6 is fully buildable with no scraping. Map any status other than 'allowed' to USAGE_LIMIT. CAVEAT: this proves 2 concurrent sessions work and that the limit signal is readable; it does NOT prove behaviour at the limit, since utilization stayed at 0.11 throughout."}
{"id": 4, "title": "S3 spike - how a write outside a card's path lease is detected", "status": "done", "acceptance_criteria": "a working mechanism is demonstrated (hook on agent writes, or git status diffed against the lease at commit time) with a test showing an out-of-lease write caught", "verify_command": "test -f docs/spikes/S3-findings.md && echo OK", "blocked_reason_code": null, "last_verified_commit": null, "notes": "CONFIRMED. A PreToolUse hook matched on Edit|Write reads file_path from the payload on stdin and exits 2 to block. Verified: the in-lease edit was written, the out-of-lease edit was refused and the file left unchanged on disk. Refusal is distinguishable three ways - hook_response with exit_code 2 / outcome error / our LEASE_CONFLICT: message on stderr, result.permission_denials carrying tool_name + tool_use_id + the full tool_input including file_path, and the agent's own accurate report. IMPORTANT: the run still ends subtype=success is_error=false, so a lease block is NOT a card failure - the board decides whether to park the card or extend the lease, and must not infer it from exit status. SECOND FINDING: the first attempt never reached the lease because the agent's opening move was `git checkout -b` (it inherits ~/.claude/CLAUDE.md, which forbids working on main), that Bash call was outside --allowedTools, and the card STOPPED AND ASKED. That is the AGENT_QUESTION blocked state and it is cleanly detectable - question in result.result, blocked call in permission_denials. Also proves --settings ADDS to the operator's global hooks/CLAUDE.md/permissions rather than replacing them."}
{"id": 5, "title": "phase 0 - ui_base foundations (v0.2.0)", "status": "done", "acceptance_criteria": "five new components in ui_base with domain-free names: bucket layout with 2D roving focus, expander, count badge, focus marker, hazard stripes. each meets the verifies: block in docs/PLAN.md. asset tests cover the new files. a demo page exercises all five", "verify_command": "cd ../ui_base && uv run pytest && uv run ruff check .", "blocked_reason_code": null, "last_verified_commit": "e29f59f", "notes": "HARD CONSTRAINT from the brief: every visual primitive lives in ui_base, smortboard only configures them. no domain nouns - not card, board, kanban or agent - in class names, ids, storage keys or comments. NOTE .rubber-band is already taken in ui_base (it is the shift-drag selection net), so the focus motion is .focus-marker. ui_base has no build step and no npm; these are plain script globals, not modules. additive only, so a consumer project needs no pin bump. BUILT 2026-09-09 on ui_base branch feature/board-primitives (e29f59f), 34 tests pass, ruff clean. Files: buckets.js (makeBuckets, roving tabindex, clamps into shorter buckets, onExitTop callback), expand.js (makeExpander, reuses .modal-backdrop/.panel-floating, a `closing` flag guards the double-dismiss when escape and an outside click both fire), indicate.js (indicateBadge + indicateFocus sharing ONE .focus-marker element moved by transition - recreating it per call made two rapid moves overlap). base.css gained .count-badge, .focus-marker, .hazard-stripes/.hazard-label. VERIFIED INDEPENDENTLY: no domain vocabulary in any new file, no import/export in assets, no colour literals outside rgba(), .rubber-band untouched except a comment explaining the distinction. Two spec gaps the agent decided: exit-top is a plain callback not a CustomEvent (no natural element to dispatch from, since the container holds many buckets), and the expanded panel sizes off 90vh clamped by 90vw since the spec fixed the ratio but not the size."}
{"id": 6, "title": "phase 1 - the board, no agents", "status": "done", "acceptance_criteria": "sqlite schema + migrations; `smortboard export` round-trips a portable bundle; http server serving own ui/ first with ui_base fallback; card CRUD with every section 7 field except telemetry; kanban columns wired from phase 0 components; the full keyboard model; docker image; state survives a container restart", "verify_command": "uv run pytest && docker compose up -d && curl -sf localhost:8000/health", "blocked_reason_code": null, "last_verified_commit": null, "notes": "deliberately has no agent execution - that way every failure in phase 2 is unambiguously about execution rather than about the board. all keyboard bindings resolve on event.code not event.key: the physical key position is layout-independent and only the printed character moves. brackets were rejected as panel keys because they need AltGr on european layouts; it is , for workforce and . for mission control. PHASE 1 SHIPPED AND MERGED 2026-09-09: store, server, interface, docker, all ship criteria met including container restart with state intact. A design pass followed on branches feature/gold-attention-hue (ui_base) and feature/cream-cards-and-gold-attention (smortboard), BOTH PUSHED AND AWAITING MERGE - merge ui_base FIRST, smortboard pins its sha. PALETTE NOW: three plate colours - fill-good=kingfisher-milk, fill-warn=stone-red-lift, fill-attention=vanilla. The working plate is COLD ON PURPOSE: green against red is the one pairing that collapses under red-green colour blindness. The three sit 47.6/49.5/63.2 apart; the green set they replaced had a closest pair of 19.6. lichen-milk was retired. burnt-orange exists and is deliberately unused, held in reserve for something unmissable. THREE TIMES a bug hid behind green tests because the js suites did not load a ui_base script or the dom stub could not model the real DOM: the drawer that computed perfect geometry and never appended itself, indicateFocus possibly undefined, and makeDrawer missing from the binding stub. Both suites now load indicate.js and the drawer stub asserts document.body containment. CLOSED 2026-09-09: shipped, merged, and running. The board, the store, the API and docker are all on main."}
{"id": 7, "title": "phase 2 - one card executes end to end", "status": "done", "acceptance_criteria": "one card runs unattended, gets its own worktree and branch in the card's repo, commits, and reaches Checking or Blocked with a correct reason code. stop button and 'pause and review with orchestrator' both work", "verify_command": "uv run pytest tests/test_exec.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "All three spikes confirmed - phase 2 is unblocked. PHASE 2 IS SMALLER THAN PLANNED - session lifecycle (--background, claude agents/logs/stop/rm), worktree creation and commit detection are all CLI features, not code we write. One ADDED requirement from S1: cut the worktree and branch ourselves and hand the agent a checkout already on the right branch, because a headless agent inherits the global playbook and picks its own branch name otherwise. blocked reason codes: CRASH, USAGE_LIMIT, LEASE_CONFLICT, AGENT_QUESTION, TESTS_FAILED, REVIEW_REJECTED, DEPENDENCY_REJECTED. no mid-flight card edits - stop, review, restart. ADDED by S3: decide explicitly what a card run inherits from the operator's global config (--setting-sources, --system-prompt) rather than letting it be ambient; the personal playbook is written for an interactive operator who can answer a prompt, and a card agent has nobody to ask. BUILT 2026-09-09 on branch feature/phase2-runner, PR #10, 40 tests pass. worktrees.py cuts the worktree AND its branch in one `git worktree add -b` so the checkout is already on its branch; leases.py writes the lease, guard and settings INSIDE the card's own worktree (verified: no path in exec/ touches the operator's own config dir); runner.py streams every line into append_event and reads result / vcs_state_changed / rate_limit_event. classify_result checks a lease conflict BEFORE is_error, because S3 showed a blocked write still ends subtype=success. Stop is a managed subprocess (SIGTERM then SIGKILL) rather than a background session, which would mean polling a second process for logs. CONFIG SCOPING DECIDED: --setting-sources project drops the user-level settings and playbook, plus an explicit --system-prompt supplying the two facts the playbook would otherwise give. UNVERIFIED: the accepted values of --setting-sources and --system-prompt were never spiked - one real invocation should confirm before phase 3 leans on it. CLOSED 2026-09-09 - PROVEN AGAINST THE REAL BINARY, not just fixtures. Two cards run end to end: (1) the plan's whole-system test - worktree cut and checked out on card/<id>, 11 turns, 39 events recorded into the store, committed, acceptance met (prints 0.3.1, exit 0). (2) a card whose lease covered only cli.py but whose brief asked for README.md too: README UNCHANGED, cli.py changed and committed, permission_denials naming the refused path, and subtype=success is_error=False - confirming a lease block is NOT a card failure. The agent's own summary said it noted the block and did not retry, which settles the --setting-sources project + --system-prompt scoping that was previously unverified. Cost about $0.12 per card. TWO THINGS THE RUNS EXPOSED: the allowlist is too narrow for a card to run its own tests (two Bash denials), and card commits do not follow the house style because the playbook is deliberately stripped. Both are addressed on feature/card-scoping-and-tasks."}
{"id": 8, "title": "phase 3 - the gates (tests, reviewer, merge request)", "status": "done", "acceptance_criteria": "the whole-system test case in docs/PLAN.md passes end to end: card runs, tests execute and pass, reviewer approves, Checking shows worker summary + reviewer verdict, merge button yields a github PR link, rejection cuts a fresh worktree and attaches the prior diff", "verify_command": "uv run pytest tests/test_whole_system.py", "blocked_reason_code": null, "last_verified_commit": "4c4fc0f", "notes": "DONE 2026-09-11: the reject path ran for real. a card on the scratch board went run -> reject -> run -> reject: attempt 1 stopped as AGENT_QUESTION with nothing committed (so nothing to attach, no PR to close); attempt 2 cut a FRESH worktree from base 4c4fc0f, opened PR #35, and rejecting it attached attempt-1.diff, closed #35 with its branch kept on github, and left no local worktree or branch. $0.92 in all. with last night's accept of run 4's card, every acceptance item of phase 3 has now happened for real. WATCH ITEM from attempt 1: the agent found the base commit's object unreadable INSIDE ITS CLONE and stopped with a clear question instead of doing .git surgery (the guard refused its rm). the repo itself checked clean (cat-file ok, fsck exit 0). unverified hypothesis: the clone raced a `git pull` into local main seconds earlier. if it recurs, look at the clone step in ContainerBackend._clone. ACCEPT EXERCISED FOR REAL 2026-09-10: the operator accepted run 4's card from the board, it moved to accepted, and he merged PR #28 (smortboard --version). LEFT for acceptance: the reject path for real - a card rejected from the board closes its PR, attaches the attempt diff, and the next run cuts a fresh worktree. RUN 4, 2026-09-10 night - THE FIRST CARD END TO END: worker $0.24 over 15 turns, the board's test gate 6 passed on the card's own commit, the reviewer approved ($0.19, one low finding: the version lookup runs on every cli start), and the board opened PR #28 itself. its commit is authored smortboard <smortboard@localhost>. LEFT for acceptance: the decision step for real - accept (y) or reject (x) PR #28 from the board. run 4 used the scratch board on port 8044 and the draft repo image from task 16. RUN 3, 2026-09-10 late - first real card to authenticate and work in its container (runs 1-2 died on the settings path, then a truncated 80-byte token; the full one is 108). the agent implemented --version within the lease and committed 88b49d5 (39 turns, $0.88), then the card blocked AGENT_QUESTION before any gate ran. FOUND, all measured: (a) task 18 - the gates test STALE files: after fetch-back the worktree HEAD is 88b49d5 but its files are the old version (git diff HEAD: 20 deletions; the offline gate on it: 5 passed, and smortboard --version unrecognized). (b) task 19 - the exact test_command was denied though Bash(<test_command> *) is allowlisted and the docs say a trailing ' *' matches the bare command. (c) task 20 - the repo CLAUDE.md, loaded via --setting-sources project, orders ruff before every commit while the allowlist refuses ruff: 16 denials, most of them ruff. (d) task 21 - no git identity in the container; the agent invented smortboard-agent <agent@smortboard.local>. (e) task 22 - AGENT_QUESTION fired because the summary contained the word 'approval', not because the agent asked anything. the card's worktree and branch card/e6b11adb-fb50-4d57-a80b-e08d2f96bdb0 are KEPT as evidence. REOPENED 2026-09-10 evening - the first real card run crashed, so the fake-boundary verify was not the whole story. what the run found, all measured: (1) CRASH in 27s before the model started - `Settings file not found: <host worktree>/.claude/settings.json`; the container backend passed the host path to --settings and only the clone is mounted at /workspace. (2) the guard hooks were wired as `{sys.executable} {host path}` and neither smortboard-card nor any image built on it had python3; a PreToolUse hook exiting 127 is non-blocking per the hooks docs, so inside a container the lease and bash guards could never have refused anything - the container backend had simply never run a real card. (3) the reviewer passed the same host settings path. FIXED on smortboard fix/container-card-settings (c6b149c, pushed, not merged): guards written with in-container paths, mounted read-only at /smortboard outside /workspace, root read from lease.json, python3 in card.Dockerfile. proven in a real container: lease allows smortboard/cli.py and refuses README.md exit 2, bash guard refuses /etc/passwd exit 2, /smortboard is read-only. 185 passed. STILL OWED: the real run itself, after the fix merges, the card image is rebuilt, task 15 is decided and task 16 exists. the run used a scratch board db, the card from docs/PLAN.md's whole-system case with lease smortboard/cli.py + tests/test_cli.py, and test_command `uv run --no-sync python -m pytest -p no:cacheprovider -q tests/test_cli.py` - NOT the full suite, since test_container_backend_real_docker_smoke fails inside a container that has no docker. QUIRK: smortboard/.claude/ (where card worktrees live) is not gitignored and shows as untracked on main. CLOSED 2026-09-10 on main 05daa2d (#25 merged feature/phase3-checking-and-rejection with feature/phase3-ui folded in). verify: tests/test_whole_system.py 6 passed; full suite 179 passed, 1 failed - test_container_backend_real_docker_smoke, only because the docker daemon was not running, not a code fault; ruff clean. WHAT IS NOT PROVEN: the whole-system test fakes the model, docker and gh. unlike phase 2, no card has yet run the gates against the real claude binary, a real container and a real PR - do that once before phase 4 leans on it. gate to Checking is BOTH unit tests passing AND reviewer approving - not either. the board never merges; it pushes and links the PR, the operator merges. ALREADY ON MAIN 2026-09-10: test gate (#21), reviewer (#22), merge request (#23), chained lifecycle + r to run a card (#24), all merged. REMAINING, built on smortboard feature/phase3-checking-and-rejection (python) and feature/phase3-ui (board.js, merged into the first): (1) worker summary + test result + reviewer verdict + PR link projected from the event log and shown in the card panel; (2) accept (y) and reject (x); reject attaches base...branch as a read-only attempt diff, closes the PR with gh (never merges), keeps the remote branch, destroys the local worktree and branch; the next run cuts fresh from base; (3) findings route per card, fix or attention; (4) dependents of a rejected card get DEPENDENCY_REJECTED + attention; (5) tests/test_whole_system.py over real http + real git, with fakes only at the model/docker/gh boundary. DECIDED BY THE OPERATOR 2026-09-10: the global findings_route FORCES all cards when set, unset means each card decides (default attention); auto-fix gets 2 rounds, then the card blocks REVIEW_REJECTED; rejecting closes the PR and keeps its branch; y accepts, x rejects. BUG FOUND: lifecycle set status=checking BEFORE the gates, so a gate-blocked card sat in the Checking column, against the rule that Checking needs both gates. moved to after approval. NAMING TRAP: the cards.review_flag column is the ATTENTION flag (lifecycle sets it on every block), not the brief's 'review flag' - the new setting is named findings_route to stay clear of it."}
{"id": 24, "title": "the open-card layout: two-up with a state edge and a run timeline", "status": "done", "acceptance_criteria": "the chosen layout's rules live in layout.css for every card, panel-layouts.css and the layout-N preview hook in board.js are gone, and the panel still wraps with zero clipped lines", "verify_command": "node tests/js/card_panel.mjs && a headless chromium pass counting clipped lines in the open panel (0)", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED 2026-09-11 in round two (smortboard #36 #37 #38, ui_base #18 #19). PICKED BY THE OPERATOR 2026-09-10: 7 two-up + 11 state edge + 14 run timeline, combined as the default in layout.css for every open card. the fifteen stay as a DESIGN ARCHIVE at his request ('I would love to be able to go back and reminisce about design decisions'): panel-layouts.css, the layout-N hook in board.js, and the 'panel layouts' board. that board is copied durably to ~/Library/Application Support/smortboard/design-archive.db (open it with `smortboard --db` on that path); the session scratch db it was built in will vanish. the archive css starts each variant from a reset, since the new default is a grid. all on smortboard feature/card-panel-layouts-and-flip, folded into its single commit. BUILT 2026-09-10 on smortboard feature/card-panel-layouts-and-flip: fifteen variants in smortboard/ui/panel-layouts.css, one card each on a 'panel layouts' board, three per column: to do = ruled list, ledger, grouped rules; doing = boxed, inset fills, boxed groups; checking = two-up, main and sidebar, header trio; accepted = header band, state edge, outcome first; rejected = tiles, run timeline, compact. every card carries run 4's real content. a card wears variant N when its workstream is layout-N (openCardPanel). the board lives in the session scratch db realrun4.db on port 8044 - a scratch path that can vanish; if it does, re-seed from any card with a real run. FOUND ON THE WAY: ui_base's .field-value is white-space nowrap, which clipped every long line - the panel wrapper is .section-value now. MY READ, unasked: 11 state edge, 14 run timeline and 2 ledger read best; 8, 9, 10 needed grid-area mapping to work at all."}
{"id": 25, "title": "running a decided card: refuse it, and never let a refused rerun hide the real outcome", "status": "done", "acceptance_criteria": "r on an accepted or rejected card is refused with a clear message before any worktree is cut, and the panel keeps showing the last run that actually produced an outcome", "verify_command": "uv run pytest -k 'decided_card and run' && node tests/js/card_panel.mjs", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED 2026-09-11 in round two (smortboard #36 #37 #38, ui_base #18 #19). BUILT 2026-09-11 by an agent on smortboard fix/rerun-accepted-card (c83bbc8), reviewed: an accepted card is refused right after get_card, BEFORE lifecycle_started is recorded, so the refusal starts no attempt; card_outcome walks back to the latest attempt with a worker_summary/test_gate/review_gate/merge_request. rejected cards stay re-runnable (tested). 247 passed. SEEN 2026-09-10 late on run 4's accepted card: r was pressed on it, and each run was refused with `git worktree add -b card/63188de2-... fatal: a branch named ... already exists` - accepting keeps the branch for its pull request, so a fresh worktree cannot be cut. worse, outcome.card_outcome reads only the latest attempt (after the last lifecycle_started), so the refused rerun made the panel say 'not run yet' over a card whose real run passed its gates and opened PR #28. two fixes: lifecycle refuses decided cards up front, and the outcome skips attempts that never reached the worker."}
{"id": 18, "title": "sync the card's worktree after fetch-back, so the gates test the card's commit", "status": "done", "acceptance_criteria": "after a container run the worktree's files equal its HEAD (git diff HEAD is empty), so the test gate and the reviewer's context see the card's commit; a test runs the real clone-then-fetch-back path, not a worker that commits in the worktree", "verify_command": "uv run pytest tests/test_backends.py -k fetch_back", "blocked_reason_code": null, "last_verified_commit": "69173f4", "notes": "MERGED as smortboard #29; proven by run 4. BUILT on smortboard fix/real-run-3-findings, pushed, awaiting the operator's merge; proven by run 4. HIGHEST PRIORITY - a false pass. backends.py _fetch_back uses git fetch --update-head-ok into the branch the worktree has checked out, which moves the ref but leaves index and files. its comment says the worktree is 'never used mid-run' - false: lifecycle runs run_test_gate(tree.path) and run_review(tree.path) right after. likely fix: git -C <worktree> reset --hard after the fetch (the agent never writes the worktree, only the clone; untracked .claude guards survive). the whole-system test's Worker commits straight into the worktree, which is why nothing caught it."}
{"id": 19, "title": "the card can run its repo's own test_command", "status": "done", "acceptance_criteria": "a real card run issues the exact test_command and it is not in permission_denials", "verify_command": "a small container probe: claude -p asked to run the exact command, with the board's --allowedTools, and an empty permission_denials", "blocked_reason_code": null, "last_verified_commit": "69173f4", "notes": "MERGED as smortboard #29; proven by run 4. ROOT CAUSE FOUND, not the allowlist: run 3's refusal text was 'PreToolUse:Bash hook error: [python3 /smortboard/bash_guard.py]: BASH_ESCAPE: /test_cli.py is outside this card's worktree'. the guard's /\\S+ regex read the slash inside the relative path tests/test_cli.py as an absolute path. probed in a container with sonnet: Bash(<cmd> *) allowed the exact command, colon and all; so did the exact rule and a colon-free command. the guard only became effective with the container fix (python3 + in-container paths) - before that it failed open. FIX: check only paths starting at a word boundary, and let /dev/null through. PROBE QUIRK: the token file has no trailing newline, so a hand-rolled `read -r && ...` chain stops before claude; the board is fine because it sends token + newline. the card image's claude is 2.1.197 from a cached npm layer, host is 2.1.267. BUILT on smortboard fix/real-run-3-findings, pushed, awaiting the operator's merge; proven by run 4. run 3 denied `uv run --no-sync python -m pytest -p no:cacheprovider -q tests/test_cli.py`, the repo's exact test_command, though runner.allowed_tools_for_repo grants Bash(<test_command> *). the permissions docs: 'A * at the end, with a space before it, also matches the bare command' when it is the only wildcard, and a colon is only special as a :* suffix. so the docs say it should match - cause UNKNOWN. prove small first: probe Bash(<cmd>) exact vs Bash(<cmd> *), with and without the colon. cheap fix regardless: grant both forms."}
{"id": 21, "title": "the board sets the card's git identity", "status": "done", "acceptance_criteria": "a card commit carries an author the board chose, set in the clone before the run, and the agent never needs git config", "verify_command": "git log --format='%an <%ae>' main..card/<id> shows the chosen identity", "blocked_reason_code": null, "notes": "MERGED as smortboard #29; proven by run 4. DECIDED BY THE OPERATOR: a fixed identity set by the board - smortboard <smortboard@localhost>, in ContainerBackend._clone. BUILT on smortboard fix/real-run-3-findings, pushed, awaiting the operator's merge; proven by run 4. the card image has no git identity, so `git commit` hard-failed and the agent set a local one it made up: smortboard-agent <agent@smortboard.local>. DECISION for the operator: who authors card commits - a fixed smortboard identity, one per card, or the operator. set it in ContainerBackend._clone with git -C <clone> config.", "last_verified_commit": "69173f4"}
{"id": 20, "title": "a card can run the repo's lint, or stops being told to", "status": "done", "acceptance_criteria": "a card never spends turns on a command its instructions require and its allowlist refuses", "verify_command": "a real card run's permission_denials contain no ruff command", "blocked_reason_code": null, "notes": "MERGED as smortboard #29; proven by run 4. DECIDED BY THE OPERATOR: a lint_command on the repo, allowlisted like test_command, split on && since each subcommand must match; told to the card verbatim in commands_preamble. check-only by convention. BUILT on smortboard fix/real-run-3-findings, pushed, awaiting the operator's merge; proven by run 4. the repo CLAUDE.md (lines 63, 102) says run ruff before every commit, and cards load it through --setting-sources project, but the allowlist is git + test_command only - run 3 spent most of 16 denials on ruff. OPTIONS: a lint_command on the repo, allowlisted like test_command (and run by the board as a gate?); or tell the card in SYSTEM_PROMPT that lint is the board's job, not its own.", "last_verified_commit": "69173f4"}
{"id": 22, "title": "AGENT_QUESTION only when the agent actually asked", "status": "done", "acceptance_criteria": "a result whose summary merely mentions 'approval' while reporting denied commands does not block AGENT_QUESTION; a real question still does", "verify_command": "uv run pytest tests/test_exec.py -k agent_question", "blocked_reason_code": null, "last_verified_commit": "69173f4", "notes": "MERGED as smortboard #29; proven by run 4. BUILT on smortboard fix/real-run-3-findings, pushed, awaiting the operator's merge; proven by run 4. runner._agent_question_signal is denials + any of '?', 'ok to proceed', 'approval' in the result text. run 3's summary said commands were 'blocked with This command requires approval' - a report, not a question - so the card stopped before the gates, which would have run the tests themselves. denials the board expects (a refused lint) should not stop the chain at all."}
{"id": 23, "title": "each card carries a model, planned by the orchestrator, overridable by the operator", "status": "done", "acceptance_criteria": "a nullable cards.model (null = board default) reaches the card's claude --model; the card panel shows and edits it; the orchestrator sets it when it creates a card", "verify_command": "uv run pytest -k card_model", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED 2026-09-11 in round two (smortboard #36 #37 #38, ui_base #18 #19). FRONTEND DONE 2026-09-11 on feature/model-per-card (e5d6c8d): `m` cycles the card's model; FOUND AND FIXED: the server kept a stale second copy of the writable card fields and answered `model` with 400 - it imports the store's CARD_WRITABLE_FIELDS now. the same branch carries the operator's asks from that hour: a two-column shortcut overlay, and a usage panel in a card's language with ui_base's fill bar (ported from a consumer project's review tool to ui_base feature/fill-bar 27b8632; menus raised above the focus marker on ui_base fix/menus-over-focus-marker f1a3f54, pinned). merge order: ui_base fill-bar, ui_base fix, smortboard prompt-editor, rerun fix, then this. BACKEND BUILT 2026-09-11 on smortboard feature/model-per-card (a8ef0c9): migration 7 cards.model; worker = card model, else setting worker_model, else sonnet; reviewer = setting reviewer_model, else sonnet (separate, as recommended); the orchestrator proposes a model per card and a non-name is dropped with a board note (it reaches `claude --model`). budget stays $5 per card for every model - the scaling question is still open. ALSO FIXED THERE: get_card never returned attachments, so the panel's attachments section always said none (found by the real reject). LEFT: the board-side control to see and change a card's model, after the prompt editor lands (both edit board.js). ASKED BY THE OPERATOR 2026-09-10. today every card runs 'sonnet' hard-coded as the default of runner.build_command and ContainerBackend.run_card. the column and the override are small and can land before phase 4; the orchestrator half belongs to phase 4. OPEN: does the reviewer get its own setting (recommended - a cheap worker should not mean a cheap review), and does the $5 card budget scale with the model."}
{"id": 15, "title": "read the card token from a 600 file, so macOS never prompts", "status": "done", "acceptance_criteria": "with ~/.config/smortboard/card_token present (mode 600), the board reads the token with no keychain prompt and never calls the keychain; with no file it falls back to the OS credential store", "verify_command": "PYTHON_KEYRING_BACKEND=keyring.backends.null.Keyring uv run pytest tests/test_backends.py -k 'file_wins or fallback_without'  then, warned first: start the board, GET /api/runtime, and no popup appears", "blocked_reason_code": null, "last_verified_commit": "aebb796", "notes": "DONE: file-first merged as #27; the keychain entry is deleted; runs 3 and 4 read the 108-byte token from the file with the runtime ready. DECIDED BY THE OPERATOR 2026-09-10: option B, the 600 file - 'I am running it for me only, we can get creative later'. this reverses the 2026-09-09 move of the token off disk, deliberately. BUILT on smortboard fix/card-token-file-first: read_card_token reads the file first and only asks the credential store when there is no file; an explicit token_path never reaches the keychain; the missing-token message and README lead with the file. NOT BUILT, proposed only: refusing a token file that is group or world readable, the way ssh does. REMAINING, the operator's own terminal (the token must not pass through a session): mkdir -p ~/.config/smortboard && (umask 077; security find-generic-password -s smortboard-card-token -a smortboard -w > ~/.config/smortboard/card_token). unverified hypothesis: that read raises no popup, since security created the entry. optionally then security delete-generic-password -s smortboard-card-token -a smortboard, so a missing file fails loudly instead of prompting. FOR LATER, if smortboard ever ships as a package: a keychain grant trusts the interpreter, not smortboard, so every python script on that interpreter gets the token too; only a developer-id-signed app gets a real grant-once (unverified here); /usr/bin/security reads avoid prompts for any install. 2026-09-10: every keyring read (backends.py _credential_store_token, via /api/runtime and at run start) raised a macOS popup and ruined a consumer project's recording. cause: the entry was created by /usr/bin/security, so only security is on its ACL; the board reads via python keyring from uv's cpython-3.13.13, which is ad-hoc signed (codesign: Signature=adhoc, TeamIdentifier not set). unverified hypothesis: 'Always Allow' binds to that binary and is lost on each uv python upgrade. OPTIONS put to the operator: A (recommended) on darwin shell out to /usr/bin/security find-generic-password -w, keep keyring for windows/linux - stays in keychain, no prompt, any same-user process could also read it silently, about the exposure of a 600 file; B 600 file at ~/.config/smortboard/card_token, already a supported fallback, reverses the 2026-09-09 off-disk decision; C keep keyring and click Always Allow. CORRECTION to the operator's premise: claude code itself uses the keychain on macOS too; its .credentials.json 0600 file is linux/windows, or macos only when the keychain rejects the write. NEVER start the board or anything reading the keychain without warning the operator first - he may be recording."}
{"id": 16, "title": "smortboard's own repo image, so the test gate can run offline", "status": "done", "acceptance_criteria": "docker/repo.Dockerfile is committed and documented; built as smortboard-repo:latest it runs the repo's test_command with --network none, the version metadata and the smortboard script exist, and the mounted clone is what imports", "verify_command": "docker run --rm --network none -v <clone>:/workspace:ro -w /workspace smortboard-repo:latest sh -c 'uv run --no-sync python -m pytest -p no:cacheprovider -q tests/test_cli.py'", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #33. BUILT 2026-09-11 as docker/repo.Dockerfile on smortboard feature/repo-image (2524ab4): built from the committed file and run against a fresh clone with --network none, `smortboard --version` printed 0.1.0 and tests/test_cli.py passed 6. run 4 ran on this draft image and its gate passed 6 on the card's commit. PROVEN 2026-09-10, not committed (the draft lives only in a session scratchpad): the stock smortboard-card image cannot run smortboard's tests in the gate - `uv run pytest` with --network none dies on `dns error` fetching cpython. the working draft: FROM smortboard-card:latest; ENV UV_PROJECT_ENVIRONMENT=/opt/venv UV_PYTHON_INSTALL_DIR=/opt/python UV_LINK_MODE=copy; WORKDIR /workspace; COPY pyproject.toml uv.lock and smortboard/; RUN uv sync --frozen && rm -rf /workspace/*. the editable install matters: dependencies-only left importlib.metadata.version('smortboard') and the smortboard script missing in the gate, so a correct --version could pass in the agent's container and fail the gate. measured with it: version 0.1.0 from metadata, smortboard imported from /workspace, script runs, tests/test_cli.py 5 passed offline. rebuild it whenever uv.lock changes."}
{"id": 9, "title": "phase 4 - mission control (orchestrator)", "status": "done", "acceptance_criteria": "a conversation in mission control produces cards on the board that pass phase 3. role-layered prompts (orchestrator/worker/reviewer) versioned in the store and editable in the UI. orchestrator keeps its own plan ledger reconciled against cards", "verify_command": "uv run pytest tests/test_orchestrate.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #34; the prompt editor followed in #36. BUILT OVERNIGHT 2026-09-11 on smortboard feature/phase4-mission-control (one squashed commit; supersedes feature/p4-backend and feature/p4-frontend). SHIPS-WHEN MET FOR REAL: an orchestrator turn (opus, 10s) turned one sentence into a card with criteria and a lease; that card ran through the board to PR #32 (agent $0.25, 7 turns, zero denials; reviewer $0.17; tests passed). its workforce transcript was narration only: 'Adding the docstring to main().' ... 'Lint and tests pass. Committing the change.' PANELS per the operator ('just like terminals ... conversational'): `.` mission control, `,` workforce (focused card, else open, else roster; notes reach the agent on its NEXT run), `a` roster (activity derived from the latest tool use), `u` usage (window status + reset; NO utilisation - today's rate_limit_event has none). prompt layering: role = unit, versioned in the store, seeded from the code constants, editable over the API (docs/PROMPTS.md) - NOT YET IN THE UI. found in the first real render and fixed: drawer keys typed themselves into the input; workforce resolved its card after the input took focus; [hidden] lost to display:flex. NOT BUILT: a prompt editor in the UI; attention indicator; live input into a running session (would need --input-format stream-json). built by two sonnet agents against one written contract; their own flagged gaps: the event half of the conversation view had no direct test (it now has a real-run proof, still no unit test), unmatched depends_on names are dropped silently, two stale comments in orchestrator.py. prompts layer by ROLE ONLY - the operator was explicit that per-board, per-workstream, per-card and per-model layering are not wanted. cards stay ground truth; the orchestrator ledger is how it plans, not a second record of state. it creates cards directly without approval; the operator amends after."}
{"id": 10, "title": "phase 5 - parallelism", "status": "in_progress", "acceptance_criteria": "three cards across two repos run concurrently and all three merge cleanly. seat contention parks a card as USAGE_LIMIT rather than failing it. resume briefing hands a card its event log summary instead of re-ingesting the worktree, and is toggleable; an agent roster popup (top-bar button, key `a`) lists every agent currently holding a card with its one-line activity, disabled rows for blocked cards, and picking a row opens that card", "verify_command": "uv run pytest tests/test_parallel.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "REAL-REPO RUN 2026-09-12: three smolsmort cards (housekeeping, classdefs, train.py) ran concurrently on a real repo with a remote. one reached a pull request (smolsmort #9, classdefs, in checking); two blocked on board faults, not their work (tasks 52 and 53). round four merged as #41. what is still unproven: 'all three merge cleanly'. BUILT 2026-09-11 on smortboard feature/round-four (PR pending). PROVEN FOR REAL: three haiku cards across two scratch repos (alpha: a1.txt, a2.txt; beta: b1.txt) started together by the scheduler at max_parallel 3, all running at the 10 s mark, each committed only its leased file, passed the test gate and the reviewer, and stopped at the pull request because the repos have no remote - $0.14 in all, 22-34 s each. the acceptance's 'all three merge cleanly' needs real remotes. RESUME BRIEFING built (smortboard/briefing.py): a re-run's brief carries the last attempt's outcome, gate verdicts, findings, files, commands and refusals; setting resume_briefing=off disables it; it no longer counts the run asking as an earlier attempt and does not claim old commits after a reject's fresh cut. SCHEDULER BUILT as task 27 (PR #39); what is left here is the real three-card concurrent run and the resume briefing. blocked on S2. the resume briefing is the operator's own idea from question 2 and is worth building well: the point is that resuming a card should not mean blasting tokens re-reading the whole worktree, since the event log already knows what changed, what is next and which file to open. ROSTER (added 2026-09-08 at the operator's request): needs NO new ui_base component - a Menu `list` row already carries label + stats + on + disabled + action, which is exactly a roster row. The one-line activity is DERIVED from the event stream's most recent tool use, never asked of the agent - the stream already says it, so asking would spend tokens and latency to learn nothing new. Placed in phase 5 because with one card running the board already tells you everything the roster would. Key `a`; no collision with u/s/g/i//,./1-9."}
{"id": 11, "title": "phase 6 - telemetry and workforce panel", "status": "queued", "acceptance_criteria": "token counts reconcile against the event log for a completed card. workforce panel defaults to the result view with the live stream a second press. telemetry section per card toggled with i. usage dropdown scoped to what S2 proved readable", "verify_command": "uv run pytest tests/test_telemetry.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "LARGELY BUILT: usage (u, phase 4) and per-card telemetry (i, task 29, PR #39). PHASE 6 IS MUCH SMALLER THAN PLANNED. Telemetry is a projection of the result event's usage/modelUsage; the usage dropdown and quota meter are a projection of rate_limit_event's unifiedWindows. Neither needs instrumentation of our own and neither needs status-line scraping. The workforce panel DELIBERATELY inverts brief section 4.4: the brief says 'I mostly use this to watch' but section 1 says watching an agent type is a failure mode. the operator accepted the inversion - result view first, live stream on a second press."}
{"id": 12, "title": "make an unfanned card readable as a distinct card", "status": "done", "acceptance_criteria": "with the fan collapsed, a stack of overlapping cards reads as separate cards at a glance - a viewer can count them and tell where one ends and the next begins, without focusing any of them", "verify_command": "open the fan board at /ui/index.html and look at the collapsed stack", "blocked_reason_code": null, "last_verified_commit": null, "notes": "Raised by the operator 2026-09-09 after seeing the fan working: 'Fan looks great but I can't tell that they are individual cards when unfanned.' The fan itself is settled and liked - focused card stays put, siblings below slide down, pure css off :focus so the arrow keys already drive it. The problem is only the COLLAPSED state: the overlap was 96px HIDDEN of a 264px card, so two thirds stayed visible and the stacking barely showed - raised to 176px for the comparison, and consecutive bands read as one ruled list rather than as a pile of cards. Nothing has been tried yet - deliberately left open. Options not yet evaluated: a shadow or a darker edge on the overlapping side so each card casts onto the one beneath; a slight horizontal stagger; letting the visible sliver keep the footer instead of the title; a thicker top border on the overlapped portion. WHEREVER THIS LANDS, the fan belongs in ui_base rather than smortboard - it is an interaction over a list and knows nothing about cards, which is exactly what the hard constraint says lives in the package. It sits in smortboard now only because it is still a preview, on branch feature/card-layout-preview. FIVE OPTIONS BUILT 2026-09-09 on feature/overlap-options, PR #11, preview not for merge: gap (dark seam), frame (heavier overlapped edge), tilt-out and tilt-in (perspective on the column, rotateX on the card, focus flattens it to 0deg), and cast (shadow onto the card beneath). The preview neutralises the status border so only the treatment varies, and the treatment name rides in the title because the title band is the only part a collapsed card shows. CLOSED 2026-09-09. the operator chose gap plus cast: an 8px band of the page's ground between cards, with a shadow that wraps the top corners - wide's blur with cornered's positive spread at 0.72. The spread must be POSITIVE or the shadow rises off the top edge and never turns the corner. The overlap is also now a proportion (-80% of width, which is two thirds of the card's height) rather than a fixed 176px, which had the visible strip shrinking with the column - 106px of card at 1600 wide against 43px at 1100. Both the card and the fan now live in ui_base."}
{"id": 13, "title": "scope a card's tools per repo, let tasks tick, and give cards the house conventions", "status": "done", "acceptance_criteria": "repos carry a test_command and the runner derives its allowlist from it; a PreToolUse Bash guard refuses commands reaching outside the card's worktree; set_task_done exists and acceptance criteria have no update path; the runner's system prompt carries a short conventions block", "verify_command": "uv run pytest", "blocked_reason_code": null, "last_verified_commit": null, "notes": "Decided by the operator 2026-09-09 as the three things Phase 3 runs into. TOOLS: scoped per repo, not outside the repo - his words. Enforceable for file tools via the lease hook; only heuristic for Bash, because shell is arbitrary and a determined command gets out. Real containment means running the card in a container, which docker already makes plausible and which is a later decision. The guard must say so in its own comment rather than implying it is a boundary. TASKS TICK, CRITERIA DO NOT: criteria are the contract the reviewer judges against, so an agent that could edit them could move its own goalposts. CONVENTIONS: a short block owned and versioned by smortboard - lowercase past-tense commits, no emojis, lowercase comments, python via uv run - NOT the whole playbook, which is what deadlocked S3 by asking a question nobody was there to answer. Built on feature/card-scoping-and-tasks."}
{"id": 14, "title": "windows support: written but never run", "status": "queued", "acceptance_criteria": "a colleague on windows installs docker, runs claude setup-token, stores it in credential manager, and runs a card end to end - or the platform is documented as unsupported", "verify_command": "run a card on a windows machine; there is no way to verify this from macos", "blocked_reason_code": null, "last_verified_commit": null, "notes": "OFF PRIORITY, the operator 2026-09-09 - recorded so nobody invests in it AND nobody claims it works. The code paths exist and are not speculative work to be removed: the credential store uses keyring (keychain / credential manager / secret service, one api), the fallback token path follows the platform (APPDATA vs XDG_CONFIG_HOME), and the missing-credential message names the store the operator actually has. The guards the board writes are python and the only `sh` is inside the container, which is linux whatever the host is, so nothing else is unix-specific. WHAT IS UNVERIFIED: none of it has run on windows. the platform branches are tested by faking os.name, which proves the logic picks the right path, not that credential manager behaves as expected. docker desktop on windows also translates paths between windows and its linux vm, and the clone-and-bind-mount has been reasoned about rather than observed. a colleague's first run is the real test."}
{"id": 17, "title": "an expired card token blocks with a clear reason", "status": "done", "acceptance_criteria": "a card run whose token is expired or revoked blocks with a dedicated reason code and a comment saying to rerun claude setup-token, not a generic CRASH", "verify_command": "uv run pytest -k expired_token", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #41 (round four); RunResult.auth_failed on main reads api_error_status 401 (runner.py:376). BUILT 2026-09-11 on smortboard feature/round-four (PR pending). MEASURED with a bogus token in the card image (free): the result event carries is_error true, api_error_status 401, 'Failed to authenticate. API Error: 401 OAuth access token is invalid.'. RunResult.auth_failed reads api_error_status 401 and the lifecycle REFUSES the card with how to renew the token, rather than blocking it CRASH. a dedicated reason code was not added: the schema CHECK lists the codes, and a refusal is exactly 'the board could not run this card'. 2026-09-10: grepping smortboard/ for expir/401/unauthori finds only process timeouts, so nothing detects an expired token. the operator's card token was made 2026-09-10 and lasts one year, so it expires around 2027-09-10. renewal: claude setup-token, then security add-generic-password -U -s smortboard-card-token -a smortboard -w"}
{"id": 26, "title": "attention inbox: answers resume blocked cards (key n)", "status": "done", "acceptance_criteria": "built on its own branch from base/round-three, tested, checked in a real browser, and merges cleanly", "verify_command": "uv run pytest && node tests/js/*.mjs", "blocked_reason_code": null, "last_verified_commit": "3f73182", "notes": "MERGED as smortboard #39 (ui_base #20). BUILT 2026-09-11, integrated on smortboard feature/round-three, PR #39 (merge ui_base #20 first - pinned 1c6e198). attention inbox (n): attention.py rows oldest first across boards; an answer to AGENT_QUESTION / TESTS_FAILED / REVIEW_REJECTED / CRASH / LEASE_CONFLICT becomes a the operator comment and resumes the card, reusing its worktree (existing_worktree / add_worktree). review, USAGE_LIMIT and DEPENDENCY_REJECTED rows are not answerable and carry a hint instead (added in the review pass). browser-checked on 127.0.0.1:8047. THE OPERATOR 2026-09-11: 'Start working on all 5, I love them.' the top-right attention counter from the brief; a panel lists every card needing the operator with the agent's actual question; an inline answer is stored as his note and the card resumes. needs lifecycle to REUSE a blocked card's existing worktree instead of refusing to cut one"}
{"id": 27, "title": "overnight throughput: parallel scheduler and a morning digest (key w)", "status": "done", "acceptance_criteria": "built on its own branch from base/round-three, tested, checked in a real browser, and merges cleanly", "verify_command": "uv run pytest && node tests/js/*.mjs", "blocked_reason_code": null, "last_verified_commit": "3f73182", "notes": "MERGED as smortboard #39 (ui_base #20). BUILT 2026-09-11, integrated on smortboard feature/round-three, PR #39 (merge ui_base #20 first - pinned 1c6e198). scheduler.py BoardScheduler (w) + digest.py (d): max_parallel default 2, starts gated on dependencies accepted, conservative lease-glob overlap, USAGE_LIMIT pause until resetsAt. REVIEW PASS FOUND A RACE: a tick wrote its leftover queue over a stop() made mid-tick; ticks now serialised with an RLock, tests/test_scheduler_race.py fails on the old code. NOT PROVEN: three real cards running concurrently (task 10's acceptance). THE OPERATOR 2026-09-11: 'Start working on all 5, I love them.' phase 5: run the board's todo cards with bounded concurrency (default 2 - S2 proved two sessions share a seat cleanly), dependency order, lease conflicts, USAGE_LIMIT parks rather than fails; then a 'since you left' digest - PRs to merge in order and the questions waiting"}
{"id": 28, "title": "live steering: a note reaches a running agent", "status": "done", "acceptance_criteria": "built on its own branch from base/round-three, tested, checked in a real browser, and merges cleanly", "verify_command": "uv run pytest && node tests/js/*.mjs", "blocked_reason_code": null, "last_verified_commit": "3f73182", "notes": "MERGED as smortboard #39 (ui_base #20). BUILT 2026-09-11, integrated on smortboard feature/round-three, PR #39 (merge ui_base #20 first - pinned 1c6e198). PROVEN FOR REAL 2026-09-11 in the card container (haiku, scratch repo, no remote): note queued at 9.2s landed between two tool calls, the agent skipped three.txt and ended with PINEAPPLE, no injection flag, exit at 12.8s, $0.055. the merged branch only wrote notes after a result event (the end of a worker's task); a feeder thread now writes each note within NOTE_POLL_SECONDS=1 and the reading thread records note_delivered. UNVERIFIED: whether total_cost_usd is session-cumulative across several result events (the proof had one). CORRECTION, SECOND SPIKE 2026-09-11 (tool-using turn, ~$0.04): on claude 2.1.197 a stream-json message written mid-turn is INJECTED BETWEEN TOOL CALLS, inside the same turn - no separate second result. the first spike's 'queued, not interrupting' only held because that turn had no tool calls. BUT haiku treated the injected note as a prompt injection ('I notice there's a suspicious instruction...') and ignored it - so the worker system prompt must announce the note protocol up front (a fixed marker, genuine, takes priority). ALSO RETRACTED: the card image's CLI is not stale from a cached layer - an uncached build installs 2.1.197 again, npm's current version; the host's 2.1.267 comes from a different install channel. the live-steering agent was sent this evidence and the prompt change. SPIKE DONE 2026-09-11 (haiku in the card image, claude 2.1.197, ~$0.06): with `claude -p --input-format stream-json --output-format stream-json`, (a) a message written AFTER the first turn's result is answered in the SAME session with its memory ('remember lichen' -> later 'lichen'); (b) a message written MID-TURN is queued, not interrupting - the first turn finished (1..40), then the queued message got its own turn ('kingfisher'). DESIGN FROM THAT: keep the worker's stdin open; after each worker result, deliver any notes the operator sent meanwhile as a follow-up turn in the same session and worktree, BEFORE the gates; close stdin only when none are pending. honest label: a note lands when the agent finishes its current step. UNVERIFIED: whether a newer CLI injects queued messages between tool calls inside a turn - the card image's CLI is stale (2.1.197 vs 2.1.267 on the host, from a cached npm layer); worth a spike on a rebuilt image. THE OPERATOR 2026-09-11: 'Start working on all 5, I love them.' UNPROVEN MECHANIC - spike first: does `claude -p --input-format stream-json` accept user messages mid-run over stdin? the container currently reads the token from stdin then redirects /dev/null. only after the spike: keep stdin open per running card and deliver workforce notes live"}
{"id": 29, "title": "cost and learning: per-card telemetry and model choice from history (key i)", "status": "done", "acceptance_criteria": "built on its own branch from base/round-three, tested, checked in a real browser, and merges cleanly", "verify_command": "uv run pytest && node tests/js/*.mjs", "blocked_reason_code": null, "last_verified_commit": "3f73182", "notes": "MERGED as smortboard #39 (ui_base #20). BUILT 2026-09-11, integrated on smortboard feature/round-three, PR #39 (merge ui_base #20 first - pinned 1c6e198). telemetry.py card_telemetry / board_costs / board_evidence (i) + the orchestrator scorecard. REVIEW PASS: worker_model came from a set and could name claude code's haiku helper ($0.0009) for sonnet's work ($0.2476); now the costliest model. telemetry panel names models by role. THE OPERATOR 2026-09-11: 'Start working on all 5, I love them.' per card: cost, turns, refusals, fix rounds, per model; the orchestrator's snapshot gains each card's history so it can propose haiku where haiku has been passing. run 3 spent $0.88 mostly on refused commands - that should be visible at a glance"}
{"id": 30, "title": "replay: a scrubbable run timeline (key t)", "status": "done", "acceptance_criteria": "built on its own branch from base/round-three, tested, checked in a real browser, and merges cleanly", "verify_command": "uv run pytest && node tests/js/*.mjs", "blocked_reason_code": null, "last_verified_commit": "3f73182", "notes": "MERGED as smortboard #39 (ui_base #20). BUILT 2026-09-11, integrated on smortboard feature/round-three, PR #39 (merge ui_base #20 first - pinned 1c6e198). timeline.py + replay.js (t): steps paired tool_use to tool_result per attempt, refused only from real permission_denials. review pass: clock times and lowercase kind label. THE OPERATOR 2026-09-11: 'Start working on all 5, I love them.' every step of a run from the stored events - tool calls with their file or command, refusals, the edits as they landed - so a run can be audited without rereading the diff"}
{"id": 31, "title": "stop a running card (key k)", "status": "done", "acceptance_criteria": "merged, with the real-run evidence in the notes", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #41. BUILT 2026-09-11 on smortboard feature/round-four (PR pending). PROVEN FOR REAL: RunRegistry.stop on a running haiku card removed its named container (smortboard-worker-<card>-<uuid>), the phase ended stopped, no gate or review ran, the card kept its worktree with review_flag and a 'Stopped by the operator.' comment. removing the container before signalling the docker client took the stop from 10.4 s to 0.2 s. the test gate still runs to completion if a stop lands inside it (subprocess.run, not interruptible)."}
{"id": 32, "title": "dependents wait for the dependency's pull request to merge, not just accept", "status": "done", "acceptance_criteria": "merged, with the real-run evidence in the notes", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #41. BUILT 2026-09-11 on smortboard feature/round-four (PR pending). gh pr view <url> --json state,mergedAt through the merge_request gh allowlist, cached 60 s; real shape checked on #28 (MERGED), #35 (CLOSED, mergedAt null), #39. a card with dependencies is cut from a fresh fetch of origin/<base>; a failed fetch falls back to the local base with a board comment. if GitHub cannot be asked, the dependent waits."}
{"id": 33, "title": "cost overview across every board (key c)", "status": "done", "acceptance_criteria": "merged, with the real-run evidence in the notes", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #41; rebuilt as one row per board in #47. BUILT 2026-09-11 on smortboard feature/round-four (PR pending). telemetry.boards_overview + GET /api/costs; the panel is the wide usage card with one ruled section per board (spend, share bar, runs, accepted, prs, cost per pr, worker/reviewer split, refusal spend, spend by model) and a jump list. mission-control turns are not counted - the orchestrator stores no cost, and the panel says so. rebuilt after the first version clipped every line."}
{"id": 34, "title": "open card sections headed like board columns, not boxed", "status": "done", "acceptance_criteria": "merged, with the real-run evidence in the notes", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #40. THE OPERATOR 2026-09-11: sections as a title and a divider, with enough space that the negative space forms the boxes. smortboard PR #40 (feature/card-open-sections), checked on 127.0.0.1:8048."}
{"id": 35, "title": "small fixes found by round four's real runs", "status": "done", "acceptance_criteria": "merged, with the real-run evidence in the notes", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #41. BUILT 2026-09-11 on smortboard feature/round-four (PR pending). a card refused at the pull request step read as 'in progress' in telemetry - the lifecycle now records run_refused and run_stopped events and telemetry reads them; the inbox names stopped and refused runs instead of calling them a review; the worker brief names /workspace up front (a real run spent two tool calls finding it); cost and turns are summed across a session's results (measured per turn: $0.0087 then $0.0129 in one session, each num_turns 1)."}
{"id": 36, "title": "polish: operator name, chat layout, timeline dots by meaning, mall cam, cost table", "status": "done", "acceptance_criteria": "merged, checked in a real browser", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": "b1e69a2", "notes": "MERGED as smortboard #47. the operator's name comes from SMORTBOARD_OPERATOR_NAME, else git config user.name, else 'the operator' (smortboard/operator.py) and reaches prompts, lifecycle lines, /health and the ui. timeline dots: lichen milk done, stone red problem, blue doing, vanilla attention. the unpinned workforce rotates every 8 s (mall cam). a blocked run's spend is attributed by the event before it (telemetry _result_role)."}
{"id": 37, "title": "the alpha: pre-flight (h), boards and repos manager (b), readme art, bug template", "status": "done", "acceptance_criteria": "a colleague can install, check, register a repo and run a card without curl; walked from an empty database in a real browser", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": "d92c6d5", "notes": "MERGED as smortboard #50 (identical tree to #48, which was merged into #47's branch 12 s after #47 landed and never reached main; #49 superseded and merged harmlessly). the first-run walk found creating the FIRST board threw: the empty state removed the columns it renders into (tests/js/first_board.mjs). UNPROVEN: nobody has installed it on a second machine yet."}
{"id": 38, "title": "ui from use: drawer keys, chat dots, text boxes, usage %, overlay row, focus lag, follow every run", "status": "done", "acceptance_criteria": "merged, each checked in a real browser or measured", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED: #52 (, and . open without focus, / to type, escape back), #53 (a dot per chat name, text two spaces in, no log box, textareas centre their line via smortui #24, a usage window's % through), #54 (the overlay's nine board keys as one row), #55 (focus frame lag: stale 400 ms timers from earlier targets dragged it back - samples off the focused card 11/11/14 -> 0 after smortui #25), #56 (the board polls /api/runs every 4 s and redraws, so a run not started with r still moves its card)."}
{"id": 39, "title": "merge requests: push a re-run to the open PR, and sum every run into the cost line", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest tests/test_merge_request.py -q", "blocked_reason_code": null, "last_verified_commit": "23ae4b1", "notes": "MERGED as smortboard #57. FOUND ON THE FIRST REAL-REPO CARD (smolsmort #4): a second attempt fixed the reviewer's finding (043e444), passed both gates, and was never pushed - open_merge_request returned on an existing PR before pushing. and the PR said '$0.30 over 3 turns' (the reviewer's last result) for $5.11 over 53. 435 tests pass."}
{"id": 40, "title": "decide how a card PR shows its tasks", "status": "queued", "acceptance_criteria": "a card PR never arrives with boxes that nothing will tick", "verify_command": "uv run pytest tests/test_merge_request.py -q", "blocked_reason_code": "NEEDS_DECISION", "last_verified_commit": null, "notes": "merge_request._body renders tasks as checkboxes from the board's task.done, and nothing ticks them during a run, so every card PR arrives unticked - the operator called merging #4 like that 'dirty'. options: a plain list ('what the card was given'), or tick only from a verifier step. never from the agent's own claim. on #4 I checked each against the diff and ticked them by hand."}
{"id": 41, "title": "the boards panel and api can edit a repo's lint command and default branch", "status": "queued", "acceptance_criteria": "PATCH /api/repos/{id} and the b panel accept lint_command and default_branch", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "FOUND 2026-09-13 by a consumer-project session, measured: smolsmort's repos.default_branch stayed 'feature/review-tool' after that branch was merged into smolsmort main (#2), so the classdefs card's PR #9 merged into it at 06:24Z and its 3 commits never reached main - they wait in smolsmort #10 (feature/review-tool -> main). PATCH /api/repos/<id> still takes only image and test_command, so moving the base means a direct db write; the consumer-project session's attempt was denied by its permission check and nothing was written. the live value is still 'feature/review-tool' - the operator's call. PATCH takes only test_command and image. smolsmort's lint command and its default branch (feature/review-tool, the integration branch) were set with sqlite directly on 2026-09-11. 2026-09-13 consumer-project-5e: the default-branch half is built in smortboard PR #70 (feature/repo-default-branch-api): PATCH /api/repos/<id> takes default_branch, validated with validate_repo before any write. the lint-command half and the boards panel are still open. after #70 merges and the board restarts: PATCH smolsmort's repo to default_branch main."}
{"id": 42, "title": "watch: a card branch someone else pushed to diverges from the board's local copy", "status": "queued", "acceptance_criteria": "the board notices and says so before a run, instead of failing the push at the end", "verify_command": "uv run pytest -q", "blocked_reason_code": null, "last_verified_commit": null, "notes": "on smolsmort's design card the board's worktree holds 043e444 while origin has 6703ee0 and b32ac0b (my fixes to #4). with #57 a re-run pushes to the open PR, unforced, so it would be refused - correct, but late. do not re-run that card."}
{"id": 43, "title": "refuse a card with no lease, and make a lease settable", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #60, 2026-09-12/13. 7 of 10 smolsmort cards had an empty lease; the classdefs card spent $1.91 and 61 turns probing for a writable path. the lifecycle refuses an unleased card before a worktree is cut; Store.set_leases + PATCH /api/cards/<id> {leases}; the orchestrator is told to lease every card and flags one without; the card panel shows the lease."}
{"id": 44, "title": "block a card a dead board left mid-run as CRASH when the next board starts", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #61, 2026-09-12/13. every run writes run_ended; build_server blocks a doing, unflagged card with no run_ended since its last start as CRASH with a note; telemetry reads run_orphaned as CRASH. a dry run on a copy of the live db recovered nothing."}
{"id": 45, "title": "serialize git writes per repo so concurrent card starts cannot fail on a lock", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #62, 2026-09-12/13. housekeeping was refused 'could not lock config file .git/config', started 17 ms after classdefs. repo_lock around worktree add/remove, branch -D, fetch, fetch-back and push; worktrees cut --no-track. reproduced 6 of 12 concurrent cuts failing, 0 after."}
{"id": 46, "title": "mark flagged cards on the board, not only cards with a reason code", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #63, 2026-09-12/13. a refused card has review_flag and no code; the inbox counted it and the board drew it plain."}
{"id": 47, "title": "apply the lease-overlap rule to manual runs and inbox answers", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #64, 2026-09-12/13. found by the smolsmort session: POST /run and answer_card called runs.start directly, and run-all only counted its own runs. scheduler.conflicting_run covers all three; a manual overlap answers 409."}
{"id": 48, "title": "headless cards never wait on a wake-up; a commitless run stops before its gates", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #65, 2026-09-12/13. housekeeping backgrounded pytest, set Monitor and ScheduleWakeup, said 'pausing here' and ended - 12 writes lost; the board then spent ~9 min gating and reviewing the unchanged tree. --disallowedTools for the waiting tools, the bash guard refuses run_in_background, runner.HEADLESS_RULES appended to every worker prompt (stored or default), and the lifecycle refuses a run with no commit before the gates."}
{"id": 49, "title": "let cmd, ctrl and alt through to the browser instead of firing board shortcuts", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #66, 2026-09-12/13. reported by a colleague: copy/paste did not work. cmd+c opened the cost panel and cmd+r ran the focused card."}
{"id": 50, "title": "say what to do next on every surface a waiting card shows up on", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #67, 2026-09-12/13. the operator: needed actions identifiable from anywhere. smortboard/actions.py is one table; inbox rows lead with 'next:', the strip footer shows the short action, the open card leads with it, every board note ends 'Next: ...'; line breaks kept; worker/orchestrator defaults take the ACTION/WHY/DONE/NOT DONE and GOAL/SCOPE/OUT OF SCOPE/RULES shapes already stored on the live board."}
{"id": 51, "title": "pin ui_base to smortui main 3ea805c", "status": "done", "acceptance_criteria": "merged", "verify_command": "uv run pytest && for f in tests/js/*.mjs; do node $f; done", "blocked_reason_code": null, "last_verified_commit": null, "notes": "MERGED as smortboard #68, 2026-09-12/13. every consumer moved to the same full sha 3ea805c331a000e057a490cef661735e8d7003c3: smortboard #68, smolsmort #8, a consumer project's PR #110 (smolsmort there at 2269799). ce6c002..3ea805c was README only."}
{"id": 54, "title": "warn when a repo's gate image is older than its uv.lock", "status": "queued", "acceptance_criteria": "preflight flags a repo whose image predates its uv.lock or lacks a locked dependency", "verify_command": "uv run pytest tests/test_preflight.py -q", "blocked_reason_code": null, "last_verified_commit": null, "notes": "found by the smolsmort session 2026-09-12: the gate runs --no-sync offline, so a dependency a card adds fails its own gate until someone rebuilds the image by hand. smolsmort-repo was rebuilt twice this session for ui_base."}
{"id": 56, "title": "a card can check formatting but never apply it", "status": "queued", "acceptance_criteria": "decide: grant the formatter's write form, or accept check-only and tell the card so", "verify_command": "a real card run's permission_denials contain no ruff format command", "blocked_reason_code": "NEEDS_DECISION", "last_verified_commit": null, "notes": "the Bash grants derive from test_command's parts, so `ruff format --check ...` is allowed and `ruff format <file>` is denied (train.py card, 2026-09-12). granting the write form lets a shell command reformat files outside the lease, which is why it was not done."}
{"id": 58, "title": "every card agent gets codegraph by default: the worktree indexed and the MCP server wired in", "status": "queued", "acceptance_criteria": "a card run, with no per-repo setup, finds the codegraph MCP tools (codegraph_explore, codegraph_node) available and the worktree already indexed: the card image ships the codegraph cli, the runner indexes the worktree before claude starts (codegraph init/index, or update when .codegraph exists), passes an --mcp-config for `codegraph serve` and allows its tools, and keeps .codegraph out of the card's commits. verifies: a card on a repo with no .codegraph/ calls codegraph_explore successfully in its transcript, and its PR diff contains no .codegraph files. a repo can opt out.", "verify_command": null, "blocked_reason_code": null, "last_verified_commit": null, "notes": "operator 2026-09-13, relayed by a consumer-project session (proposed id codegraph-by-default): codegraph built in by default. the global playbook's 'indexing is my decision' rule is answered by this default. MEASURED on smortboard main acd4c39 by both sessions: runner.py:46 SETTING_SOURCES = 'project' drops ~/.claude, where codegraph's MCP is wired globally, so card agents never see it; docker/card.Dockerfile (node:20-slim + git, curl, ca-certificates, python3, uv, @anthropic-ai/claude-code) does not install it; this machine has codegraph v1.0.0 (~/.codegraph/versions/v1.0.0, symlinked from ~/.local/bin) with init, index, serve (MCP server) and install. --setting-sources project is deliberate (the personal playbook deadlocks headless cards, per S3), so wire codegraph through the card's own --mcp-config and --allowedTools rather than re-admitting ~/.claude. index time on a large repo adds to card start-up; measure it on a consumer project before deciding index-per-card vs a shared cached index."}
{"id": 59, "title": "create a board from a repo online: pick a github repo, clone it, register it", "status": "queued", "acceptance_criteria": "the boards panel's 'from online repo' button (the third in its first row, disabled today) lists the operator's github repos through gh, clones the picked one into a chosen local folder, and hands it to the same path as 'from local repo' (POST /api/boards/from-repo), so the board is named after it and the repo is registered with its default branch. verifies: picking a repo with no local clone ends with a board, a clone on disk and the repo row on that board; a gh that is missing or logged out is refused with the fix.", "verify_command": null, "blocked_reason_code": null, "last_verified_commit": null, "notes": "operator 2026-09-13: the create board dialogue gets three buttons in its first row - new board, from local repo (built now, feature/board-from-local-repo), from online repo (this task). consumer-project-5e. the from-local path already validates and detects the default branch; this task only adds listing and cloning in front of it."}
{"id": "mc-store-links", "title": "card_links table: a card may link to one (repo path, task id), unique", "status": "queued", "acceptance_criteria": "a second link to the same task raises TaskAlreadyLinked naming the holder and leaves one card; export then import keeps the link; create_card accepts a given card_id", "verify_command": "uv run pytest -q tests/test_store_links.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/store/{schema,api,errors,export}.py, tests/test_store_links.py. after: -. model: sonnet. build as a board card once the board runs again; smortboard/store/{schema is the unit's main file."}
{"id": "mc-ledger-files", "title": "read and write TASKS.jsonl and CARDS.jsonl, with the status mappings", "status": "queued", "acceptance_criteria": "this repo's dev_ledger/TASKS.jsonl round-trips byte-identical; a junk line is reported by number; accepted maps to done, done tasks are never proposed", "verify_command": "uv run pytest -q tests/test_ledger_files.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ledger/{__init__,files}.py, tests/test_ledger_files.py. after: -. model: sonnet. build as a board card once the board runs again; smortboard/ledger/{__init__ is the unit's main file."}
{"id": "mc-scrub", "title": "scrub terms from ~/.config/smortboard/scrub.txt, with a residue check", "status": "queued", "acceptance_criteria": "'ACME ships projectx' with terms acme and 'ProjectX => a private project' becomes '[redacted] ships a private project' with no residue; a missing file raises naming its path", "verify_command": "uv run pytest -q tests/test_scrub.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ledger/scrub.py, tests/test_scrub.py. after: -. model: sonnet. build as a board card once the board runs again; smortboard/ledger/scrub.py is the unit's main file."}
{"id": "mc-repo-snapshot", "title": "read-only shallow clones of each repo's default branch, mounted at /repos/<name>", "status": "queued", "acceptance_criteria": "the clone has no .claude/worktrees and no card branches; every -v ends :ro; -w is /repos; the temp dir is gone after exit", "verify_command": "uv run pytest -q tests/test_repo_snapshot.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/exec/repo_snapshot.py, tests/test_repo_snapshot.py. after: -. model: sonnet, opus review. build as a board card once the board runs again; smortboard/exec/repo_snapshot.py is the unit's main file."}
{"id": "mc-lease-check", "title": "check a lease against the repo's tracked files; a card with an invalid glob cannot run", "status": "queued", "acceptance_criteria": "with pkg/a.py tracked: pkg/*.py ok (1 match), pkg/new.py ok (folder exists), src/**/*.tsx invalid; a card leased to it is refused naming that glob", "verify_command": "uv run pytest -q tests/test_lease_check.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/lease_check.py, smortboard/lifecycle.py, tests/test_lease_check.py. after: -. model: sonnet. build as a board card once the board runs again; smortboard/lease_check.py is the unit's main file."}
{"id": "mc-docs-security", "title": "README roles and security, the containment revision, scrub.txt, ledger modes, keys", "status": "queued", "acceptance_criteria": "no 'has no tools' or 'never sees the filesystem' left; README names scrub.txt and Read, Grep, Glob", "verify_command": "git grep -n 'has no tools' -- README.md docs/ ; test $? -eq 1", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: README.md, docs/PLAN.md, docs/PROMPTS.md. after: -. model: sonnet. build as a board card once the board runs again; README.md is the unit's main file."}
{"id": "mc-orchestrator-scope", "title": "mission control spans every board by default, or one; sees open and linked tasks; creates linked cards once", "status": "queued", "acceptance_criteria": "a fake runner proposing task 1 twice creates one card, and the second turn's board message names the card holding task 1; scope board hides other boards", "verify_command": "uv run pytest -q tests/test_orchestrator.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/orchestrator.py, tests/test_orchestrator.py, tests/test_orchestrator_scope.py. after: mc-store-links, mc-ledger-files. model: opus. build as a board card once the board runs again; smortboard/orchestrator.py is the unit's main file."}
{"id": "mc-ledger-sync", "title": "sync ledger: CARDS.jsonl and task statuses on a smortboard/ledger-sync branch, as a PR", "status": "queued", "acceptance_criteria": "against a bare origin: the branch holds CARDS.jsonl, an accepted card's task is done, origin's default branch is unchanged; a scrub residue opens nothing", "verify_command": "uv run pytest -q tests/test_ledger_sync.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ledger/sync.py, smortboard/review/merge_request.py, tests/test_ledger_sync.py. after: mc-store-links, mc-ledger-files, mc-scrub. model: sonnet. build as a board card once the board runs again; smortboard/ledger/sync.py is the unit's main file."}
{"id": "mc-ledger-recreate", "title": "recreate cards from CARDS.jsonl with their original ids", "status": "queued", "acceptance_criteria": "three rows give the same three ids with leases, criteria, deps and links; doing becomes todo; a second call skips all three", "verify_command": "uv run pytest -q tests/test_ledger_recreate.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ledger/recreate.py, tests/test_ledger_recreate.py. after: mc-store-links, mc-ledger-files. model: sonnet. build as a board card once the board runs again; smortboard/ledger/recreate.py is the unit's main file."}
{"id": "mc-lease-approve", "title": "a lease conflict parks with the wanted paths; one approve widens, remembers and resumes", "status": "queued", "acceptance_criteria": "a run refused on x/y.js (telemetry target /workspace/x/y.js) shows wants [x/y.js]; approve adds it to the lease and the repo memory and resumes", "verify_command": "uv run pytest -q tests/test_lease_approve.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/attention.py, smortboard/exec/runner.py, smortboard/server/app.py, tests/test_lease_approve.py. after: mc-lease-check. model: sonnet. build as a board card once the board runs again; smortboard/attention.py is the unit's main file."}
{"id": "mc-orchestrator-readonly", "title": "mission control's container mounts the clones read-only with Read, Grep, Glob only", "status": "queued", "acceptance_criteria": "the captured command allows Read, Grep, Glob, disallows Edit, Write and Bash, mounts only :ro clones under /repos and no host repo path", "verify_command": "uv run pytest -q tests/test_orchestrator_runner.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/orchestrator.py, tests/test_orchestrator_runner.py. after: mc-orchestrator-scope, mc-repo-snapshot. model: sonnet, opus review. build as a board card once the board runs again; smortboard/orchestrator.py is the unit's main file."}
{"id": "mc-http-scope", "title": "the mission control route takes scope all or board", "status": "queued", "acceptance_criteria": "scope x returns 400; scope board reaches the turn", "verify_command": "uv run pytest -q tests/test_mission_control_http.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/server/app.py, tests/test_mission_control_http.py. after: mc-orchestrator-scope. model: sonnet. build as a board card once the board runs again; smortboard/server/app.py is the unit's main file."}
{"id": "mc-ledger-consolidate", "title": "consolidate: a per-row diff of db, CARDS.jsonl and TASKS.jsonl, one pick per row", "status": "queued", "acceptance_criteria": "after one pick per differing row, every row reports no difference", "verify_command": "uv run pytest -q tests/test_ledger_consolidate.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ledger/consolidate.py, tests/test_ledger_consolidate.py. after: mc-ledger-recreate. model: opus. build as a board card once the board runs again; smortboard/ledger/consolidate.py is the unit's main file."}
{"id": "mc-repo-profile", "title": "per-repo memory of approved paths and gate settings, kept in dev_ledger/REPO.json", "status": "queued", "acceptance_criteria": "a path approved with remember on one card lets another card on the repo write it without parking; after a db loss, board from repo restores test_command, image and remembered paths", "verify_command": "uv run pytest -q tests/test_repo_profile.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/store/{schema,api}.py, smortboard/exec/leases.py, smortboard/ledger/profile.py, tests/test_repo_profile.py. after: mc-store-links, mc-lease-approve. model: sonnet. build as a board card once the board runs again; smortboard/store/{schema is the unit's main file."}
{"id": "mc-ledger-http", "title": "routes for linked cards, sync, recreate, diff and pick", "status": "queued", "acceptance_criteria": "a second card for a linked task returns 409 naming the holder; a missing scrub file returns 409 with its path", "verify_command": "uv run pytest -q tests/test_ledger_http.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/server/app.py, tests/test_ledger_http.py. after: mc-http-scope, mc-ledger-sync, mc-ledger-recreate, mc-ledger-consolidate. model: sonnet. build as a board card once the board runs again; smortboard/server/app.py is the unit's main file."}
{"id": "mc-ui-mission-scope", "title": "'.' opens mission control over every board, shift+'.' over this one", "status": "queued", "acceptance_criteria": "shift+period then send posts scope board; the header says which", "verify_command": "node tests/js/mission_control.mjs", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ui/board.js, tests/js/mission_control.mjs, tests/js/keyboard_bindings.mjs. after: mc-http-scope. model: sonnet. build as a board card once the board runs again; smortboard/ui/board.js is the unit's main file."}
{"id": "mc-ui-ledger-panel", "title": "shift+l: the board's ledger panel with picks, sync and recreate", "status": "queued", "acceptance_criteria": "a pick posts {key, source}; a sync refusal shows its text", "verify_command": "node tests/js/ledger.mjs", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ui/{ledger.js,index.html,board.js}, tests/js/ledger.mjs. after: mc-ledger-http, mc-ui-mission-scope. model: sonnet. build as a board card once the board runs again; smortboard/ui/{ledger.js is the unit's main file."}
{"id": "mc-ui-leases", "title": "l: the lease editor with match counts; one-key approve in the inbox; repo settings in the boards panel", "status": "queued", "acceptance_criteria": "approve posts {paths, remember: true}; an invalid glob renders striped with 'matches nothing'", "verify_command": "node tests/js/leases.mjs", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: smortboard/ui/{leases.js,index.html,board.js,boards.js,inbox.js}, tests/js/leases.mjs. after: mc-lease-approve, mc-repo-profile, mc-ui-ledger-panel. model: sonnet. build as a board card once the board runs again; smortboard/ui/{leases.js is the unit's main file."}
{"id": "mc-ledger-system-test", "title": "whole-system test: import once, refuse a duplicate, sync, lose the db, recreate", "status": "queued", "acceptance_criteria": "3 queued tasks give 3 linked cards; a second import gives 0; a forced duplicate is 409; sync puts 3 CARDS rows on a branch; after the db is deleted, recreate restores the same 3 ids, leases and links", "verify_command": "uv run pytest -q tests/test_ledger_system.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "plan: board-spanning mission control, ledger backup and leases that hold for any repo (2026-09-13). lease: tests/test_ledger_system.py. after: mc-orchestrator-scope, mc-ledger-sync, mc-ledger-recreate, mc-ledger-http. model: sonnet. build as a board card once the board runs again; tests/test_ledger_system.py is the unit's main file."}
{"id": "gate-caches-on-readonly-mount", "title": "the test gate points tool caches outside the read-only worktree", "status": "queued", "acceptance_criteria": "a repo whose test_command is plain `ruff format --check . && pytest -q` passes the gate on a clean tree: the gate sets RUFF_CACHE_DIR and PYTEST_ADDOPTS=-p no:cacheprovider (or mounts a tmpfs over the cache paths) so no tool writes into the :ro mount; tests/test_gates.py covers it", "verify_command": "uv run pytest -q tests/test_gates.py", "blocked_reason_code": null, "last_verified_commit": null, "notes": "found 2026-09-13 on card a8f8dee5: README's recommended gate failed with `Failed to initialize cache at /workspace/.ruff_cache: Read-only file system` - the first card to reach the gate on this repo failed on the gate itself, not its code. workaround applied on the board and in the README: --no-cache and -p no:cacheprovider in the test command. the real fix belongs in review/gates.py so any repo's plain command works."}

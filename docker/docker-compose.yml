services:
  smortboard:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      # this machine only - the api has no auth
      - "127.0.0.1:8000:8000"
    volumes:
      - smortboard-data:/data

volumes:
  smortboard-data:
